#!/bin/bash
set -a

DISCO_PORT=8989
DISCO_SCGI_PORT=4441
DISCO_ROOT="$DISCO_HOME/root/"
DISCO_MASTER_HOME="$DISCO_HOME/master/"
DISCO_USER=$USER
DISCO_LOG_DIR=$DISCO_HOME
DISCO_PID_DIR=$DISCO_HOME
DISCO_MASTER_PORT=7000
#DISCO_FLAGS=resultfs
