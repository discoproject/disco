#!/bin/bash

# This is the script used to make the debian packages provided on the
# discoproject website.  Note that the packages generated by it do not
# follow all Debian packaging guidelines, so do not use the settings
# in this script if backwards-compatibility is not an issue for you.

make clean debian-clean

# Use 'make debian' below to generate more Debian compliant packages.
# The prefix and RELSRV settings ensure backwards-compatibility with
# Disco debian packages available from discoproject.org.
make debian prefix=/usr RELSRV=/srv/disco
